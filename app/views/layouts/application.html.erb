<!DOCTYPE html>
<html>
  <head>
    <title><%=@title || 'Blog App' %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link
      rel="stylesheet"
      href="https://use.fontawesome.com/releases/v5.8.2/css/all.css"
      integrity="sha384-oS3vJWv+0UjzBfQzYUhtDYW+Pj2yciDJxpsK1OYPAYjqT085Qq/1cq5FLXAZQ7Ay"
      crossorigin="anonymous"
    />
  </head>

  <body>
    <nav class="navbar navbar-expand bg-light py-3 shadow">
      <div class="container">
        <%= link_to 'Blog App', root_path, class: 'navbar-brand' %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item d-flex align-items-center">
              <% if user_signed_in? %>
                <%= current_user.name %>
                <span class='text-primary ms-1'><%= "[#{current_user.role}]" if current_user.role == 'admin' %></span>
                <%= button_to "Log Out", destroy_user_session_path, { method: :delete, data: { turbo: false }, class: 'btn btn-outline-dark ms-2'} %>
              <% else %>
                <%= link_to 'Log In', user_session_path, class: 'btn btn-outline-dark', role: 'button' %>
              <% end %>             
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <p class="notice msj-win"><%= notice %></p>
    <p class="alert msj-win"><%= alert %></p>
    <%= yield %>
  </body>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>  
</html>
